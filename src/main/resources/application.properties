# H2 database
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.url=jdbc:h2:mem:swaggerdb
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true

# MySQL database
# spring.datasource.url=jdbc:mysql://localhost:3306/swagger
# spring.datasource.username=root
# spring.datasource.password=password
# spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect
# spring.jpa.hibernate.ddl-auto=create

# http://localhost:8080/swaggerdemo
# if this property is removed, url will be http://localhost:8080
# server.servlet.context-path=/swaggerdemo


# http://localhost:8080
# http://localhost:8080/secret
# http://localhost:8080/swagger-ui.html
# http://localhost:8080/h2-console
